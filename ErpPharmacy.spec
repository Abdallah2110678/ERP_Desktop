# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=['arabic_reshaper', 'bidi', 'bidi.algorithm', 'reportlab.pdfgen', 'reportlab.pdfbase.ttfonts', 'reportlab.lib.pagesizes', 'reportlab.lib.colors', 'sqlite3', 'pyodbc', 'import_access'],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='ErpPharmacy',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['icon.ico'],
    # version='version_info.txt',   # Arabic file properties; the build with this was flagged by Defender (Bearfoos.A!ml)
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='ErpPharmacy',
)
